# software-entropy-and-reversibility

**Parent:** [[content/L1/flexible-architecture-and-tracer-development|flexible-architecture-and-tracer-development]] — The comprehensive development strategy requires establishing flexible architecture through abstraction (hiding third-party APIs) and componentization (breaking code into parts), while adopting 'tracer development'—an iterative process where skeleton code is built end-to-end to provide immediate, visible feedback, contrasting with conventional 'dead reckoning' methods.

To analyze technical problems, developers should consider the difference between tools with a graphical user interface (GUI) and small, combinable command-line utilities used at shell prompts. When considering which set of tools is superior, developers must evaluate criteria such as orthogonality, ease of use for the intended purpose, combination ability with other tools to meet new challenges, and learning curve. Similarly, when considering language features, developers should evaluate the impact that advanced facilities like multiple inheritance (supported by C++), multiple interfaces (allowed by Java), or mixins (used by Ruby) have on system orthogonality. Furthermore, developers must determine if the impact on orthogonality differs between using multiple inheritance and using multiple interfaces, and whether the impact of delegation differs from that of inheritance.

**Reversibility and Flexible Architecture:**

In software development, critical decisions are often based on a narrow view of reality, committing the project team to a course of action that cannot be easily undone. The concept of reversibility highlights the danger of making such critical decisions. For instance, if a project team commits to using one vendor's database or a specific architectural pattern, the change might be extremely expensive or impossible to undo. The author advises that even if developers must commit to a technology, abstracting the idea of a database to simply provide persistence as a service provides the flexibility to switch vendors or technologies. Similarly, when adapting a browser-based application to a mobile app, the change should ideally only require stripping out HTML rendering and replacing it with an API, minimizing the impact on the server side. Therefore, developers should avoid making decisions that are cast in stone, instead thinking of decisions as being written in the sand, which can be wiped out by a big wave.

**Recommendations for Flexible Architecture:**

*   **Abstraction:** To maintain flexibility, developers must hide third-party APIs behind their own abstraction layers.
*   **Componentization:** Developers should break the code into components; even if the deployment happens on a single massive server, this approach is much easier than refactoring a monolithic application.
*   **Guidance:** Developers must remember that there are no final decisions, and should always enable their code to adapt to change, allowing the system to 
