# decoupling-and-reversibility-in-software-design

**Parent:** [[content/L1/advanced-software-architecture-principles|advanced-software-architecture-principles]] — The context details multiple rigorous software development methodologies, including applying DRY, achieving reversibility by abstracting external services (like databases) to simply provide persistence, and managing global resources via controlled APIs. Project planning mandates using techniques like PERT (optimistic, most likely, pessimistic time estimates) and 'Eating the Elephant' (iterative slicing), while continuous guidance requires 'Tracer Bullet' methods and utilizing advanced event architectures like Pubsub, State Machines, and Stream processing (e.g., using `rxjs` for parallel stream processing).

When analyzing technical problems, developers should first compare tools with a graphical user interface (GUI) to small, combinable command-line utilities used at shell prompts. Developers must evaluate criteria such as orthogonality, ease of use for the intended purpose, combination ability with other tools to meet new challenges, and learning curve when determining which set of tools is superior. Similarly, when considering language features, developers should evaluate the impact that advanced facilities like multiple inheritance (supported by C++), multiple interfaces (allowed by Java), or mixins (used by Ruby) have on system orthogonality. Furthermore, developers must determine if the impact on orthogonality differs between using multiple inheritance and using multiple interfaces, and whether the impact of delegation differs from that of inheritance. 

In software development, developers must also consider the concept of reversible decisions. Critical decisions, such as committing to one vendor's database or a specific architectural pattern, can be extremely expensive or impossible to undo. The concept of reversibility highlights the danger of making such critical decisions. For instance, if a project team commits to using one vendor's database or a specific architectural pattern, the change might be extremely expensive or impossible to undo. The author advises that even if developers must commit to a technology, abstracting the idea of a database to simply provide persistence as a service provides the flexibility to switch vendors or technologies. Similarly, when adapting a browser-based application to a mobile app, the change should ideally only require stripping out HTML rendering and replacing it with an API, minimizing the impact on the server side. Therefore, developers should avoid making decisions that are cast in stone, instead thinking of decisions as being written in the sand, which can be wiped out by a big wave.

To maintain flexibility, developers must follow three techniques: 1) **Abstraction:** Developers must hide third-party APIs behind their own abstraction layers. 2) **Componentization:** Developers should break the code into components; even if the deployment happens on a single massive server, this approach is much easier than refactoring a monolithic application. 3) **Guidance:** Developers must always enable their code to adapt to change, remembering that there are no final decisions.

When designing code, developers must follow the principle of **Decoupling**, which shows how keeping separate concepts separate through good design principles makes the code easier to change. The enemy of change is coupling, because it links together things that must change in parallel. This requires developers to either spend time tracking down all parts that need changing or spend time figuring out why things broke when only coupling exists.

Developers must also manage mutable external resources, such as databases, file systems, and service APIs, recognizing that any mutable external resource constitutes global data. The problem with global data is that it couples the code, potentially affecting all code in the system if the global data's implementation changes. While developers should use a mechanism to wrap these external resources behind code that the developers control, any resource that is important enough to be global should be wrapped in an API. For example, if an application uses a database, datastore, or file system, the solution is to ensure developers always wrap these resources behind code that developers control. Similarly, developers should follow the general principle that when developers write code, developers must avoid relying on self-deception regarding impossible outcomes. Instead, developers must explicitly check for every possibility that the developer thinks 
