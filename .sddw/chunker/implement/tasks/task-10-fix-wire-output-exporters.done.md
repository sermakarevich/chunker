# Task 10 Completion: Wire JsonExporter and MarkdownRenderer into Pipeline and CLI

## Summary
Wired `JsonExporter` and `MarkdownRenderer` into `Pipeline._process()` so both `run` and `resume` paths produce JSON and Markdown output after processing completes.

## Commits
- `10cc098` test(chunker): add failing tests for output wiring in pipeline (FR-10, FR-11)
- `fe69584` feat(chunker): wire JsonExporter and MarkdownRenderer into pipeline (FR-10, FR-11)

## Deviations
None

## Difficulties
None
