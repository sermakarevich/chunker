You are rewriting a document chunk to be fully self-contained. A reader should understand this chunk without having read anything before it.

Rules:
- Resolve all pronouns ("they", "it", "this approach") to explicit subjects
- Clarify any implied references using the context provided
- Prepend minimal framing if the chunk starts abruptly
- Do NOT add information that is not in the original chunk or context
- Preserve the technical accuracy and level of detail

Information persistence:
- You MUST preserve ALL specific facts, numbers, names, dates, and relationships from the original chunk
- Do NOT reduce specific facts to vague generalizations (e.g., "several metrics improved" instead of "accuracy rose from 82% to 94%")
- Do NOT omit details even if they seem minor — every concrete fact matters
- The rewritten context should be at least as information-dense as the original
- Remove academic citation markers like [7], [10, 11], [13] — they are noise without a bibliography

Visual content (when page images are attached):
- One or more page images may be attached to this message. When they are, treat them as the authoritative source for anything the text layer dropped, and fold their data into the rewritten context.
- Transcribe every data table you can see into a GitHub-flavoured markdown table, preserving its actual row and column values exactly as shown — not a summary that "a table is present".
- For every chart, graph, or figure, state its concrete numbers, data series, and axis labels (for example: "bar chart of adoption by year: 2023 = 41%, 2024 = 58%, 2025 = 73%"), not merely that a figure exists.
- Capture figure and table captions and any numeric annotations or legends shown in the image.
- If no image is attached, ignore this section and rewrite from the text alone.

## Rewrite instructions
{rewrite_instructions}

## Context from earlier processing
{context_text}

## Original chunk
{chunk_text}

Respond with JSON:
- "context": the self-contained rewrite of the chunk, with all references resolved and all facts preserved
- "summary": a 1-2 sentence summary containing specific facts, not vague generalizations
- "filename": a short descriptive slug for this chunk's content (e.g., "attention-mechanism-overview"), lowercase with hyphens, no extension